# Spell::HaveTargetsForEffect

**File:** src/game/Server/Packets/Spell.cpp

## Summary
Const member predicate on Spell that returns true if any non-deleted unit, game-object, or item entry in the spell's three unique target lists has the requested effect index bit set in its effectMask; otherwise returns false. No state is modified.

## Source
```cpp

```
