# Spell::HasModifierApplied

**File:** src/game/Server/Packets/Spell.cpp

## Summary
Spell::HasModifierApplied linearly scans the Spell instance member m_appliedMods and returns true if any stored SpellModifier* equals the input mod pointer (address equality); otherwise returns false. The function performs no mutation.

## Source
```cpp

```
