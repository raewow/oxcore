# Spell::GetCurrentContainer

**File:** src/game/Server/Packets/Spell.cpp

## Summary
Const classifier that maps this Spell instance to one of four CurrentSpellTypes slots on the caster (melee, autorepeat, channeled, or generic), using a fixed priority chain over spell metadata and runtime state.

## Source
```cpp

```
