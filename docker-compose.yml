version: '3.8'

services:
  postgres:
    image: docker.io/library/postgres:16
    container_name: oxcore-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: oxcore
      POSTGRES_PASSWORD: oxcore
      POSTGRES_DB: oxcore
      TZ: 'UTC'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U oxcore -d oxcore"]
      timeout: 5s
      retries: 10
      start_period: 10s

  mysql:
    image: docker.io/library/mysql:8.0
    container_name: oxcore-mysql
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_ROOT_HOST: '%'
      TZ: 'UTC'
    volumes:
      - mysql_data:/var/lib/mysql
      - ./sql/init-databases.sql:/docker-entrypoint-initdb.d/00-init-databases.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 5s
      retries: 10
      start_period: 10s
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

volumes:
  mysql_data:
  postgres_data:
