//! ObjectGuid - re-exported from shared::protocol::guid
//!
//! This module re-exports all GUID types from the shared protocol layer
//! so that world code uses the same concrete types as shared::messages.

pub use crate::shared::protocol::guid::*;
